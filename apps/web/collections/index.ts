import { Users } from './Users';
import { Media } from './Media';
import { Pages } from './Pages';
import { Translations } from './Translations';

export const collections = {
  Users,
  Media,
  Pages,
  Translations,
};
export const collectionsArr = Object.values(collections);
